version: '3'
services:
  dss-nginx:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: dss-nginx
    ports:
      - 0.0.0.0:1935:1935
      - 0.0.0.0:8080:80
    restart: unless-stopped
